import ModulePlaceholder from "@/src/dashboard/components/ModulePlaceholder";

export default function SchedulePage() {
  return (
    <ModulePlaceholder
      title="Schedule"
      moduleLabel="Module 11 (Publish Log)"
      description="Manual record-keeping of what you posted, where, and when. No auto-publish, no OAuth, no platform API — per the Ground Rules, that's out of scope for the whole system, not just this module."
    />
  );
}
