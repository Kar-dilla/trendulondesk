import ModulePlaceholder from "@/src/dashboard/components/ModulePlaceholder";

export default function ContentFactoryPage() {
  return (
    <ModulePlaceholder
      title="Content Factory"
      moduleLabel="Module 09 (Content Factory)"
      description="Platform-variant generation (YouTube, TikTok, IG, X) from an approved master script happens here. No generation logic is implemented in this shell."
    />
  );
}
